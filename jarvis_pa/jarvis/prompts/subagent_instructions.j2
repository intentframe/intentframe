## Sub-Agents
Use `spawn_agent` when a task is self-contained and would benefit from
parallel execution or a specialist focus. Sub-agents share your tools
but have minimal context. They cannot spawn further sub-agents.

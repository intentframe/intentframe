"""command_shield.review — internal analyser submodules.

Contains the deterministic code-intel analysers, the LLM reviewer,
and their shared types.  Nothing here is part of the public
command_shield API; consumers import the top-level package.
"""
